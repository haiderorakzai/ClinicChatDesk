const form=document.getElementById('demoForm'),err=document.getElementById('error');
ClinicLocalization.bind({country:document.getElementById('demoCountry'),dial:document.getElementById('demoDial'),currency:document.getElementById('demoCurrency'),timezone:document.getElementById('demoTimezone')});
form.addEventListener('submit',async e=>{e.preventDefault();err.style.display='none';const btn=form.querySelector('button');btn.disabled=true;btn.textContent='Preparing demo…';const d=Object.fromEntries(new FormData(form));d.currency=String(d.currency||'').toUpperCase().trim();try{const r=await fetch('/api/demo/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Could not start demo');location.href='/app?demo=1';}catch(x){err.textContent=x.message;err.style.display='block';btn.disabled=false;btn.textContent='Launch live demo';}});
