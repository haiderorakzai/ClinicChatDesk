ClinicChatDesk v2.3 OpenAI tool-loop fix

Fix: when store=false, tool continuation no longer uses previous_response_id. Instead the server replays the prior response output items plus function_call_output items in the next Responses API request. This prevents previous_response_not_found errors while keeping Responses API storage disabled.
